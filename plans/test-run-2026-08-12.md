# Test Run Report — 2026-08-12

## Summary

Full test suite (`npm test`) completed with **zero failures**.

| Metric       | Value          |
|-------------|----------------|
| Test files  | 193 passed, 2 skipped (195 total) |
| Tests       | 4752 passed, 22 skipped (4774 total) |
| Duration    | 1452s (~24 min) |
| Exit code   | 0 |

**No regressions detected.**

## Changes Since Last Run

No source code changes have landed on `main` since the previous run (2026-08-09). The codebase is unchanged.

11 open pull requests are all passing CI with no merge conflicts:
- #381: Add clear() to page caches
- #380: Extract mountTome/syncfs/syncAndUnmount into shared harness
- #379: Add close() to PreloadBackend and StorageBackend interfaces
- #378: Add onblocked handler to IdbBackend.destroy()
- #377: Replace string-matching with typed SabBufferOverflowError
- #376: Extract nodeToMeta() helper
- #375: Fix setattr dirty-marking on resize failure and restoreTree flag leak
- #374: Extract shared OPFS metadata operations into OpfsBackendBase
- #373: Narrow rename catch to ENOENT-only
- #372: Extract duplicated Rng class from fuzz tests
- #371: Extract duplicated CACHE_CONFIGS to shared harness

## Skipped Tests

Same two test files skipped as before — both are environmental/platform limitations:

### 1. `tests/adversarial/allocate-mmap.test.ts` (13 tests skipped)
- **Reason**: `mmapAlloc` requires the `emscripten_builtin_memalign` native symbol, which is not exported by the compiled WASM module used in the test harness.
- **Impact**: mmap-related code paths untested in this environment.
- **Action**: No fix needed unless mmap support becomes a priority.

### 2. `tests/conformance/enametoolong.test.ts` (8 tests skipped) + 1 test in `mkdir.test.ts`
- **Reason**: Platform-dependent ENAMETOOLONG threshold varies by filesystem and OS.
- **Impact**: ENAMETOOLONG conformance not verified — rarely hit in practice.
- **Action**: No fix needed. Expected skips.

## Conclusion

The codebase remains in a healthy state with no regressions. All 4752 executed tests pass across conformance, unit, integration, adversarial, fuzz, workload, PGlite, scribe-data, and badfs test categories.
