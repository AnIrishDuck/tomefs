# Test Run Report — 2026-08-26

## Summary

Full test suite completed with **zero failures**.

| Metric       | Value          |
|-------------|----------------|
| Test files  | 193 passed, 2 skipped (195 total) |
| Tests       | 4752 passed, 22 skipped (4774 total) |
| Duration    | 1162.11s (tests 3381.63s) |
| Exit code   | 0 |

**No regressions detected.** Results are identical to the previous run on 2026-08-21.

## Test Execution

Tests were run as a single `vitest run` invocation (not batched).

Wall-clock duration: ~19 minutes. The PGlite tests remain the slowest component, as they spin up real Postgres instances across multiple cache sizes per test.

## Skipped Tests

All skips are known platform limitations, unchanged from prior runs:

| File | Skipped | Reason |
|------|---------|--------|
| `tests/adversarial/allocate-mmap.test.ts` | 13 | Requires mmap support not available in test environment |
| `tests/conformance/enametoolong.test.ts` | 8 | Platform-dependent filename length limits |
| `tests/conformance/mkdir.test.ts` | 1 | Single edge case skip |

## Changes Since Last Run

Two commits have landed since the 2026-08-21 report:

- `6866dd9` — Add test run report for 2026-08-21: all 4752 tests passing (documentation only)

No source code changes since `8a5d29a` (strengthen assertInvariants with per-node _pages checks).

## Open PRs

11 open pull requests verified on this date — all CI green, all mergeable (no conflicts):

| PR | Title | Category |
|----|-------|----------|
| #381 | Add clear() to page caches for resource cleanup on close | Feature |
| #380 | Extract mountTome/syncfs/syncAndUnmount into shared test harness | Deduplication |
| #379 | Add close() to PreloadBackend and StorageBackend interfaces | Feature |
| #378 | Add onblocked handler to IdbBackend.destroy() | Bug fix |
| #377 | Replace string-matching with typed SabBufferOverflowError | Improvement |
| #376 | Extract nodeToMeta() helper to deduplicate metadata construction | Deduplication |
| #375 | Fix setattr dirty-marking on resize failure and restoreTree flag leak | Bug fix |
| #374 | Extract shared OPFS metadata operations into OpfsBackendBase | Deduplication |
| #373 | Narrow rename catch to ENOENT-only and add adversarial tests | Bug fix |
| #372 | Extract duplicated Rng class from fuzz tests into shared harness | Deduplication |
| #371 | Extract duplicated CACHE_CONFIGS to shared harness module | Deduplication |

## Action Items

None. No regressions, no new skips, no flaky tests observed. All open PRs are healthy and awaiting review.
