# Test Run Report — 2026-08-14

## Summary

Full test suite completed with **zero failures**.

| Metric       | Value          |
|-------------|----------------|
| Test files  | 193 passed, 2 skipped (195 total) |
| Tests       | 4752 passed, 22 skipped (4774 total) |
| Exit code   | 0 (all batches) |

**No regressions detected.** Results are identical to the previous run on 2026-08-09.

## Test Execution

Tests were run in four parallel batches to work within environment constraints:

| Batch | Files | Tests | Duration |
|-------|-------|-------|----------|
| Conformance + Unit | 63 passed, 1 skipped | 2201 passed, 9 skipped | 58s |
| Adversarial | 81 passed, 1 skipped | 872 passed, 13 skipped | 87s |
| Fuzz + Integration + Workload + Scribe-data + BadFS | 25 passed | 818 passed | 263s |
| PGlite | 24 passed | 861 passed | 1502s |

Wall-clock time was dominated by the PGlite tests (~25 minutes), which spin up real Postgres instances per test variant across 4 cache sizes.

## Changes Since Last Run

No code commits have landed since the 2026-08-09 report (`2b31900`). Only test report files were added. The codebase is unchanged from the last passing run.

## Skipped Tests

Same two test files skipped as all previous runs — both are environmental/platform limitations:

### 1. `tests/adversarial/allocate-mmap.test.ts` (13 tests skipped)
- **Reason**: `mmapAlloc` requires the `emscripten_builtin_memalign` native symbol, which is not exported by the compiled WASM module used in the test harness.
- **Impact**: mmap-related code paths untested in this environment.
- **Action**: No fix needed unless mmap support becomes a priority.

### 2. `tests/conformance/enametoolong.test.ts` (8 tests skipped) + 1 test in `mkdir.test.ts`
- **Reason**: Platform-dependent ENAMETOOLONG threshold varies by filesystem and OS.
- **Impact**: ENAMETOOLONG conformance not verified — rarely hit in practice.
- **Action**: No fix needed. Expected skips.

## Conclusion

The codebase remains in a healthy state with no regressions. All 4752 executed tests pass across conformance, unit, integration, adversarial, fuzz, workload, PGlite, scribe-data, and badfs test categories. No plan for fixes is needed at this time.
