# Test Run Report — 2026-07-29

## Summary

Full test suite (`npm test`) ran on the default branch. **All tests passed — no regressions detected.**

## Results

| Metric | Value |
|--------|-------|
| Test files | 193 passed, 2 skipped (195 total) |
| Individual tests | 4752 passed, 22 skipped (4774 total) |
| Duration | 1755s (~29 minutes) |
| Exit code | 0 |

## Skipped Tests

- `tests/conformance/enametoolong.test.ts` — 8 tests skipped (all tests in file)
- 1 test skipped in `tests/conformance/mkdir.test.ts`
- Remaining skips spread across other files (22 total)

These skips appear to be intentional (environment-dependent or known limitations), not regressions.

## Observations

### Stderr noise (non-failing)
The fuzz differential tests (`tests/fuzz/differential.test.ts`) emit repeated stderr messages:
```
Aborted(internal error: mmapAlloc called but `emscripten_builtin_memalign` native symbol not exported)
```
These occur during mmap operations in the Emscripten WASM module and do not cause test failures. The tests handle the abort gracefully and continue. This is expected behavior — the compiled WASM module does not export `emscripten_builtin_memalign`, so mmap falls back to the tomefs page cache path.

### Performance profile
PGlite integration tests dominate the runtime. Individual PGlite test files take 30-220 seconds each due to WASM Postgres startup overhead. The non-PGlite tests (conformance, adversarial, fuzz, unit) complete in under 5 minutes combined.

## Fix Plan

**No fixes needed.** The test suite is fully green. No regressions were found.

If future runs surface regressions, investigation should focus on:
1. The PGlite stress tests (`tests/pglite/*.test.ts`) — most likely to surface page cache or persistence issues under real workloads
2. The fuzz differential tests (`tests/fuzz/differential.test.ts`) — catches divergence between tomefs and the MEMFS reference
3. The adversarial tests (`tests/adversarial/*.test.ts`) — targets edge cases at page cache boundaries, crash recovery, and dirty tracking
