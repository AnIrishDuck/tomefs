# Test Run Report — 2026-08-24

## Summary

Full test suite completed with **zero failures**.

| Metric       | Value          |
|-------------|----------------|
| Test files  | 193 passed, 2 skipped (195 total) |
| Tests       | 4752 passed, 22 skipped (4774 total) |
| Duration    | 1728.49s (tests 5035.07s) |
| Exit code   | 0 |

**No regressions detected.** Results are identical to the previous run on 2026-08-21.

## Test Execution

Tests were run as a single `vitest run` invocation (not batched).

Wall-clock duration: ~29 minutes. The PGlite tests remain the slowest component, as they spin up real Postgres instances across 4 cache sizes per test.

## Skipped Tests

All skips are known platform limitations, unchanged from prior runs:

| File | Skipped | Reason |
|------|---------|--------|
| `tests/adversarial/allocate-mmap.test.ts` | 13 | Requires mmap support not available in test environment |
| `tests/conformance/enametoolong.test.ts` | 8 | Platform-dependent filename length limits |
| `tests/conformance/mkdir.test.ts` | 1 | Single edge case skip |

## Changes Since Last Run

One commit on this branch since `6866dd9` (the 2026-08-21 report):

- Removed 6 redundant test run reports (07-31 through 08-15), keeping only the latest (2026-08-21)

No source code changes since `8a5d29a` (strengthen assertInvariants with per-node _pages checks).

## PR Status

11 open PRs reviewed — all passing CI (8/8 checks green) with no merge conflicts and no review comments. The PRs touch independent areas (different source files and test categories) and should merge cleanly in any order.

## Action Items

None. No regressions, no new skips, no flaky tests observed.
