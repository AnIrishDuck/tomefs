# Test Run Results — 2026-07-12

## Summary

**All tests pass. No regressions found.**

- Test Files: 187 passed, 2 skipped (189 total)
- Tests: 4571 passed, 22 skipped (4593 total)
- Duration: ~36 minutes (2157s)
- Commit: `3404c4d` (Make fsync atomic via syncAll instead of separate writes)

## Skipped Tests (Expected)

| File | Count | Reason |
|------|-------|--------|
| `tests/adversarial/allocate-mmap.test.ts` | 13 | Requires `emscripten_builtin_memalign` native symbol not exported in test WASM |
| `tests/conformance/enametoolong.test.ts` | 8 | Emscripten FS limitation — ENAMETOOLONG not enforced |
| `tests/conformance/mkdir.test.ts` | 1 | Single edge case skipped |

These are all pre-existing known skips, not new regressions.

## Observations

1. **Stderr noise from fuzz tests**: The differential fuzz tests emit `Aborted(internal error: mmapAlloc called but emscripten_builtin_memalign native symbol not exported)` to stderr when random operations hit mmap paths. This is harmless — the tests catch the abort and continue. Not a regression.

2. **PGlite test slowness**: PGlite tests run 4 cache size variants (tiny/small/medium/large) × multiple scenarios, at ~6-9s per test case. This accounts for most of the runtime. Consider marking some variants as non-@fast for quicker CI feedback.

## CI Confirmation

All recent CI runs on the `main` branch (latest 10+ commits) show `conclusion: success`. The local test run matches CI behavior.

## Plan: No Fixes Required

Since no regressions were found, no fix plan is needed. The codebase is healthy at HEAD.

### Potential Improvements (Non-Blocking)

- **mmap test coverage**: The 13 mmap tests are permanently skipped because the test harness WASM doesn't export `emscripten_builtin_memalign`. To enable them, the harness build script (`scripts/build-harness.sh`) would need to export that symbol, or a mock implementation could be provided.
- **ENAMETOOLONG**: Could be implemented in `tomefs.ts` by checking path component lengths against a configurable limit (typically 255 bytes). Low priority since PGlite doesn't generate paths that long.
