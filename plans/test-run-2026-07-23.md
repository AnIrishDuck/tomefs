# Test Run Report — 2026-07-23

## Summary

Full test suite (`npm test`) ran against commit `8a5d29a` (HEAD of main). **No regressions detected.**

## Results

| Metric       | Value          |
|-------------|----------------|
| Test files  | 193 passed, 2 skipped (195 total) |
| Tests       | 4752 passed, 22 skipped (4774 total) |
| Duration    | 1175s (~20 min) |
| Failures    | **0** |

## Skipped Tests (Expected)

- `tests/adversarial/allocate-mmap.test.ts` (13 tests) — requires `emscripten_builtin_memalign` native symbol, not available in the test environment.
- `tests/conformance/enametoolong.test.ts` (8 tests) — ENAMETOOLONG behavior not testable in this Emscripten environment.
- `tests/conformance/mkdir.test.ts` — 1 test skipped (platform limitation).

These skips are pre-existing and not regressions.

## CI Status

All 30 most recent CI workflow jobs on the `ci.yml` workflow show `conclusion: success`. CI and local results are consistent.

## Slow Tests (>60s)

These PGlite stress tests dominate wall-clock time but all pass:

| Test file | Tests | Duration |
|-----------|-------|----------|
| `pglite/sequence-stress.test.ts` | 48 | 164s |
| `pglite/bulk-load.test.ts` | 48 | 156s |
| `pglite/savepoint-stress.test.ts` | 44 | 143s |
| `pglite/jsonb-stress.test.ts` | 40 | 135s |
| `pglite/join-stress.test.ts` | 48 | 135s |
| `pglite/cursor-stress.test.ts` | 40 | 128s |
| `pglite/cache-pressure.test.ts` | 28 | 99s |
| `pglite/hash-index-stress.test.ts` | 40 | 88s |
| `pglite/vacuum-stress.test.ts` | 24 | 87s |
| `scribe-data/write-patterns.test.ts` | 20 | 65s |
| `pglite/cache-pressure-persistence.test.ts` | 15 | 62s |

## Action Items

None — no regressions to fix. The codebase is in a healthy state.
