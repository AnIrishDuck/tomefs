# Test Run Report — 2026-07-05

## Summary

Full test suite: **4513 passed, 0 failed, 22 skipped** (1332s wall-clock).
Tomefs-specific tests (TOMEFS_BACKEND=tomefs): **21 passed, 0 failed**.
CI status: all recent runs green (main and feature branches, last 10 runs).

No regressions detected.

## What Ran

| Category | Files | Tests | Duration |
|----------|-------|-------|----------|
| conformance/ | 22 | ~300 | ~10s |
| unit/ | 18 | ~800 | ~30s |
| adversarial/ | 54 | ~600 | ~60s |
| fuzz/ | 14 | ~500 | ~180s |
| integration/ | 2 | ~47 | ~10s |
| workload/ | 2 | ~100 | ~2s |
| pglite/ | 18 | ~700 | ~2800s |
| scribe-data/ | 3 | ~50 | ~200s |
| badfs/ | 1 | 58 | ~1s |

PGlite tests dominate wall-clock time (~47 minutes of the 22-minute parallel run)
because each test boots a full PGlite instance.

## Skipped Tests (Expected)

- `tests/adversarial/allocate-mmap.test.ts` (13 tests) — requires `TOMEFS_BACKEND=tomefs`
- `tests/conformance/enametoolong.test.ts` (8 tests) — requires `TOMEFS_BACKEND=tomefs`
- `tests/conformance/mkdir.test.ts` — 1 test skipped (likely platform-specific)

These are intentional: they test tomefs-only behavior not present in MEMFS.
When run with `TOMEFS_BACKEND=tomefs`, all 21 pass.

## No Regressions — No Fix Plan Needed

All 4513 tests pass cleanly. CI has been green for the last 10 consecutive runs
(2026-06-26 through 2026-07-04). There are no regressions to fix.

## Observations

1. **Test suite is healthy.** The 2300+ test count in CLAUDE.md is outdated — the
   suite now has 4535 tests (4513 passing + 22 skipped).

2. **Emscripten mmap warnings are benign.** The fuzz/differential tests emit
   `mmapAlloc called but emscripten_builtin_memalign native symbol not exported`
   on stderr. These come from MEMFS (the reference FS) attempting mmap, not from
   tomefs. The tests pass because tomefs handles mmap via its own stream_ops path.

3. **The `TOMEFS_BACKEND=tomefs` tests are covered in CI.** CI has a dedicated
   "Full test suite (tomefs)" stage that runs `TOMEFS_BACKEND=tomefs npm test`,
   plus a small-cache stress stage with `TOMEFS_MAX_PAGES=4`. The skips only
   occur in the MEMFS-reference run, which is expected — those tests exercise
   tomefs-only behavior that MEMFS doesn't implement.
