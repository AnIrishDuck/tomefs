# Test Run Report — 2026-06-24

## Local Test Results (full suite, Node 22)

- **178 test files passed**, 2 skipped
- **4314 tests passed**, 22 skipped, **0 failures**
- Duration: 1587s (~26.5 minutes)
- TypeScript typecheck: clean (no errors)

No regressions detected in the current codebase.

## CI History (most recent 30 runs)

28 of 30 runs passed. Two failures, both from 2026-06-12:

### 1. `main` — "Full test suite (Node 20)" (run 27424214224)

- **Conclusion: flaky / transient.** An identical `main` run (27424219723) triggered seconds later on the same commit (`27f86fd`) passed all jobs. Logs have expired (>90 days), so the exact failure message is unrecoverable. All other jobs in the failing run (build, smoke, small-cache stress, preload conformance, Node 22 full suite) passed — only Node 20 full suite failed.
- Likely cause: CI resource pressure or a transient timing issue specific to the Node 20 runner.

### 2. `claude/adoring-ride-m1hwhe` — "Build & type-check (Node 20)" (run 27425762333)

- **Root cause: TypeScript error.**
  ```
  tests/adversarial/orphan-page-on-create.test.ts(44,34): error TS6133:
  'tomefs' is declared but its value is never read.
  ```
- **Already fixed.** The very next run on that branch (27426143162) passed. The file `orphan-page-on-create.test.ts` does not exist on current `main`, confirming the fix was applied before merge (or the branch was rebased without it).

## Action Items

**No code fixes needed.** The codebase is clean — all tests pass locally and the two historical CI failures are resolved:

1. The Node 20 flaky failure may recur. If it does, consider:
   - Adding a retry step for the full test suite in `.github/workflows/ci.yml`
   - Checking if any tests have timing-sensitive assertions that behave differently under Node 20 vs 22
   - Reviewing whether the ~66-minute runtime for "Full test suite (Node 20)" is hitting a CI timeout

2. The Node.js 20 deprecation warning in CI logs is worth noting:
   > Node.js 20 actions are deprecated... Actions will be forced to run with Node.js 24 by default starting June 16th, 2026.
   Since it's now past June 16, the CI actions may already be running under Node 24. Consider updating `actions/checkout` and `actions/setup-node` to versions that natively support Node 24 to avoid any future breakage.
