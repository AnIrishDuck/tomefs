# Test Run Report — 2026-07-07

## Summary

Full test suite (`npm test`) ran to completion with **zero failures**.

| Metric | Value |
|---|---|
| Test files | 185 passed, 2 skipped (187 total) |
| Tests | 4,513 passed, 22 skipped (4,535 total) |
| Duration | 1,160s (~19 minutes) |
| Exit code | 0 |

## Skipped Tests (pre-existing, not regressions)

- `tests/adversarial/allocate-mmap.test.ts` — 13 tests skipped. Depends on mmap+allocate interaction that requires native symbol export (`emscripten_builtin_memalign`).
- `tests/conformance/enametoolong.test.ts` — 8 tests skipped. Platform-specific `ENAMETOOLONG` behavior.
- `tests/conformance/mkdir.test.ts` — 1 test skipped.

## Regressions

None.

## Observations

1. **mmap stderr warnings**: Every fuzz and adversarial test that exercises mmap emits `Aborted(internal error: mmapAlloc called but emscripten_builtin_memalign native symbol not exported)` to stderr. These are caught and handled — tests pass despite them — but the volume is high. The Emscripten harness WASM module does not export `emscripten_builtin_memalign`, so `mmapAlloc` falls back. This is by design for the test harness but worth noting if the harness module is ever rebuilt.

2. **PGlite test dominance**: PGlite integration tests account for the majority of wall-clock time (~15 test files, each taking 120–175s). Each spawns a full PGlite instance at four cache sizes (tiny/small/medium/large). If CI time becomes a concern, these are the primary target for parallelization or selective gating.

3. **SAB bridge timeout tests**: Four tests in `sab-bridge-edge-cases.test.ts` take 2–4s each due to deliberate timeout simulation. These are correctly slow.

## Action Items

No fixes needed — the suite is green.
