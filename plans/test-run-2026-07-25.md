# Test Run Report — 2026-07-25

## Summary

**No regressions found.** All tests pass on the current HEAD (`8a5d29a`).

## Results

| Suite | Files Passed | Files Skipped | Tests Passed | Tests Skipped | Failures |
|-------|-------------|---------------|-------------|---------------|----------|
| Full suite | 193 | 2 | 4752 | 22 | 0 |
| Non-pglite (conformance, unit, adversarial, fuzz, integration, workload, scribe-data, badfs) | 169 | 2 | 3891 | 22 | 0 |
| PGlite (fk-cascade, fts-gin, upsert-trigger, table-rewrite, schema-evolution, cte-window, scribe-data) | 24 | 0 | 861 | 0 | 0 |

**Skipped files** (expected, by design):
- `tests/adversarial/allocate-mmap.test.ts` — mmap/allocate not implemented
- `tests/conformance/enametoolong.test.ts` — ENAMETOOLONG not implemented

**Runtime:** ~40 minutes (2398s wall clock, 6937s test time across parallel workers).

## CI Status

All recent CI runs on `main` are passing (last run: #843, 2026-07-16). No commits since the last green CI run have introduced failures.

## Stderr Noise

The `mmapAlloc` warnings (`Aborted(internal error: mmapAlloc called but emscripten_builtin_memalign native symbol not exported)`) appear in stderr during fuzz tests. These are expected — the Emscripten WASM module logs this when mmap is attempted but the native symbol isn't exported. The fuzz tests handle this gracefully and all pass.

## Action Items

None. The codebase is in a healthy state with no regressions to fix.
