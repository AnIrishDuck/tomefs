# Test Run Report — 2026-07-17

## Summary

Full test suite (`npm test`) passed with zero failures. No regressions detected.

```
Test Files  193 passed | 2 skipped (195)
     Tests  4752 passed | 22 skipped (4774)
  Duration  1668.91s (~28 min)
```

## Skipped Tests (Expected)

Two test files and one individual test are intentionally skipped because they
require `TOMEFS_BACKEND=tomefs` (which switches conformance tests to run against
tomefs instead of MEMFS). These are not regressions:

1. **`tests/adversarial/allocate-mmap.test.ts`** (13 tests) — tests allocate()
   and mmap() stream_ops that only exist in tomefs, not MEMFS.
2. **`tests/conformance/enametoolong.test.ts`** (8 tests) — ENAMETOOLONG
   enforcement that tomefs implements but Emscripten MEMFS does not.
3. **`tests/conformance/mkdir.test.ts`** (1 test) — likely a single
   tomefs-specific assertion within the file.

To run these, use: `TOMEFS_BACKEND=tomefs npm test`

## Fix Plan

No fixes needed — the suite is clean. Potential future work:

- **CI coverage gap**: The default `npm test` does not exercise the
  `TOMEFS_BACKEND=tomefs` path. CI already has a separate stage for this
  (see `.github/workflows/ci.yml`), but local developers may not realize
  these 22 tests exist. Consider adding a note to CLAUDE.md or the README.
- **Test duration**: The full suite takes ~28 minutes. The slowest files are
  PGlite stress tests (`cte-window-stress`, `partition-matview-stress`,
  `dirty-shutdown`, `bulk-load`, `vacuum-stress`) at 2-4 minutes each.
  If CI time becomes a concern, these could be tagged for a separate slow tier.
