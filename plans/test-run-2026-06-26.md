# Test Run Report: 2026-06-26

## Summary

**No regressions found.** Both test configurations pass cleanly.

## Results

### Run 1: Default (MEMFS reference)

```
Test Files  178 passed | 2 skipped (180)
     Tests  4314 passed | 22 skipped (4336)
  Duration  1345.99s
```

- 2 skipped files: `enametoolong.test.ts` (tomefs-only), plus expected skips
- 22 skipped tests: tomefs-only tests (ENAMETOOLONG enforcement, mkdir long-name, allocate-mmap, close-dirty-tracking, rename-descendant-dirty-tracking) and MEMFS-specific root-dir tests

### Run 2: TOMEFS_BACKEND=tomefs

```
Test Files  180 passed (180)
     Tests  4332 passed | 4 skipped (4336)
  Duration  1325.73s
```

- All 180 files pass (including tomefs-only tests like enametoolong, allocate-mmap)
- 4 skipped tests: MEMFS-specific root-dir tests (readdir root, rename root) that correctly skip under tomefs

## Test Coverage by Category

| Category | Files | Status |
|----------|-------|--------|
| Conformance (POSIX) | 34 | All pass |
| Unit | 17 | All pass |
| Integration | 2 | All pass |
| Adversarial | 30+ | All pass |
| Fuzz | 12 | All pass |
| Workload | 2 | All pass |
| PGlite SQL | 14 | All pass |
| Scribe-data | 1 | All pass |
| BadFS | 1 | All pass |

## Observations

- No stderr warnings beyond expected Emscripten `mmapAlloc` messages (benign; Emscripten's MEMFS fallback path for mmap is unused by tomefs which provides its own mmap implementation)
- PGlite tests dominate runtime (~85% of total), each cache-size variant takes 3-5s due to PGlite startup
- SAB bridge timeout tests add ~10s each (expected; they test 2-second timeout behavior)

## Action Items

None. Codebase is clean. No fixes needed.
