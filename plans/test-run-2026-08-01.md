# Test Run Report — 2026-08-01

## Summary

Full test suite (`npm test`) completed with **zero failures**.

| Metric       | Value          |
|-------------|----------------|
| Test files  | 193 passed, 2 skipped (195 total) |
| Tests       | 4752 passed, 22 skipped (4774 total) |
| Duration    | 1577s (~26 min) |
| Exit code   | 0 |

**No regressions detected.** Results are identical to the 2026-07-31 run.

## Comparison with Previous Run (2026-07-31)

| Metric       | 2026-07-31 | 2026-08-01 | Delta |
|-------------|------------|------------|-------|
| Test files  | 193 passed, 2 skipped | 193 passed, 2 skipped | No change |
| Tests       | 4752 passed, 22 skipped | 4752 passed, 22 skipped | No change |
| Duration    | 1433s | 1577s | +144s (~10% slower) |
| Failures    | 0 | 0 | No change |

The ~10% wall-clock increase is attributable to the execution environment (4-core cloud container), not a code regression. CPU time was ~88 minutes across 4 threads. No test timeouts occurred.

## Skipped Tests

Same two test files skipped as the previous run — both are environmental/platform limitations, not code issues:

### 1. `tests/adversarial/allocate-mmap.test.ts` (13 tests skipped)
- **Reason**: `mmapAlloc` requires the `emscripten_builtin_memalign` native symbol, which is not exported by the compiled WASM module used in the test harness.
- **Impact**: mmap-related code paths in tomefs are untested in this environment.
- **Action**: No fix needed unless mmap support is a priority.

### 2. `tests/conformance/enametoolong.test.ts` (8 tests skipped)
- **Reason**: Platform-dependent; the ENAMETOOLONG error threshold varies by filesystem and OS.
- **Impact**: ENAMETOOLONG conformance is not verified in this environment.
- **Action**: No fix needed. Expected skip.

## Observations

- The `Aborted(internal error: mmapAlloc called but emscripten_builtin_memalign native symbol not exported)` message appears in stderr during several test runs. This is Emscripten noise triggered when MEMFS falls back from mmap — it does not cause test failures.
- PGlite stress tests continue to dominate runtime (~70% of wall clock). Each test case spins up a real Postgres WASM instance across 4 cache configurations (tiny/small/medium/large), taking ~3-5s per configuration.
- No new commits since the 2026-07-31 run (HEAD is `8dfeeb8`), so this run confirms stability rather than validating new changes.

## Conclusion

The codebase is in a healthy state with no regressions. All 4752 executed tests pass across conformance, unit, integration, adversarial, fuzz, workload, PGlite, scribe-data, and badfs test categories. No plan for fixes is needed at this time.
